using System;
using Bakery.Models;
using System.Collections.Generic;

namespace Bakery
{
    class Program
    {
        static void Main()
        {
            Console.WriteLine("*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*");
            Console.WriteLine("Welcome to Pierre's bakery!");
            Console.WriteLine(
                "We sell bread and Pastrys. We have a deal on bread, buy 2 get 1 free! Pastries are buy 3 get 1 free!"
            );
            Console.WriteLine(
                "How many would you like?"
            );
            }
        }
    }
